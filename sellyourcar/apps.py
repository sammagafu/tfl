from django.apps import AppConfig


class SellyourcarConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'sellyourcar'
    verbose_name = "Cars For Sale (Clients Car)"
